```
정의

GRPC를 웹 브라우저에서 사용가능하도록 브라우저 지원을 추가한 스펙

GRPC-Web 사양은 HTTP/1.1과 HTTP/2 모두 지원하지만, 현재 브라우저가 HTTP/2 를 지원하지 않으므로 GRPC-Web 을 사용하여 브라우저에서 서버로 직접 통신은 불가능.

하지만, 응답 변환 proxy 서버를 통해 Request/Response 를 브라우저가 처리할 수 있도록 변환할 수 있다.
```